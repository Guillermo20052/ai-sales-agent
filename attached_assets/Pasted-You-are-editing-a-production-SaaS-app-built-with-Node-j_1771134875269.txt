You are editing a production SaaS app built with:

- Node.js
- Express
- PostgreSQL
- Stripe
- Nodemailer
- express-session

IMPORTANT RULES:
- Do NOT remove working Stripe logic.
- Do NOT break webhook logic.
- Do NOT redesign database schema.
- Keep all existing routes unless explicitly improved.
- Preserve current functionality.

--------------------------------------------------

CURRENT PROBLEM:

After Stripe payment in production, user sees:

"The app is currently not running."

This means:

- Deployment health checks may be failing
- Success redirect may be pointing to wrong domain
- Or production is not responding to /

--------------------------------------------------

YOUR TASK:

============================
1️⃣ FIX DEPLOYMENT PERMANENTLY
============================

Ensure:

package.json contains:
{
  "scripts": {
    "start": "node server.js"
  }
}

In server.js:

const PORT = process.env.PORT || 5000;

app.get("/", (req, res) => {
  res.status(200).send("OK");
});

app.listen(PORT, "0.0.0.0", () => {
  console.log(`Server running on port ${PORT}`);
});

The "/" route MUST:
- Return 200 instantly
- Not query DB
- Not use async
- Not crash

--------------------------------------------------

2️⃣ FIX STRIPE SUCCESS REDIRECT

In stripeService.js:

Ensure success_url uses BASE_URL:

success_url: `${process.env.BASE_URL}/payment-success`,
cancel_url: `${process.env.BASE_URL}/cancel`

NOT hardcoded replit dev URLs.

--------------------------------------------------

3️⃣ ADD PROPER /payment-success PAGE

Create route:

GET /payment-success

It should:

- Require session
- Show a clean dashboard-style page
- Display:

  🎉 Subscription Activated
  Agent is now LIVE
  Hosted AI link
  Embed code
  Link to dashboard

- Auto-redirect to /dashboard after 3 seconds

--------------------------------------------------

4️⃣ UPGRADE DASHBOARD UI

When user has active subscription:

Show:

- Status badge: 🟢 Active
- Hosted AI link prominently
- Embed code box
- “Copy” buttons
- Instructions for:
  Shopify
  WordPress
  Wix
  Webflow
  Instagram bio
  Google Business

When inactive:

Show:
🔴 Subscription inactive
Button → Upgrade

--------------------------------------------------

5️⃣ REPLACE HOMEPAGE WITH REAL LANDING PAGE

Replace "/" route with:

Serve public/index.html

Create modern SaaS landing page:

Sections:

- Hero:
  "Turn Website Visitors Into Paying Clients 24/7"
  CTA: Start Free Trial

- How it works (3 steps)
- Pricing ($49/month)
- Testimonials placeholder
- CTA again

BUT:

Add separate /health endpoint:

app.get("/health", (req, res) => {
  res.status(200).send("OK");
});

Ensure deployment health check still passes.

--------------------------------------------------

6️⃣ FINAL CHECK

After changes:

- App must deploy successfully
- "/" must respond 200
- "/health" must respond 200
- Stripe success must redirect to payment-success
- No breaking of login/signup
- No breaking of webhook
- No breaking of agent route

--------------------------------------------------

DO NOT REMOVE ANY EXISTING WORKING CODE.

ONLY IMPROVE + FIX DEPLOYMENT + UX.

Return final modified files clearly separated.